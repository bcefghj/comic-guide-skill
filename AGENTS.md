# Agent Rules for comic-guide-skill

When this skill is loaded, the agent should:

1. Read `SKILL.md` for the complete workflow instructions
2. Reference `references/styles.md` for style-specific CSS and character settings
3. Reference `references/html-template.md` for the HTML output template
4. Use `examples/openclaw-guide/` as a reference for the expected output quality
5. Generate self-contained HTML files with no external dependencies
6. Output vertical-layout pages optimized for mobile viewing (max-width: 480px)
